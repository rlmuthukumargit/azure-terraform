parameters:
- name: environment
  displayName: 'Environment'
  type: string
  default: 'dev'
  values:
  - dev
  - qa
  - prod

trigger:
- main
- qa

pool:
  vmImage: 'ubuntu-latest'

variables:
- group: synapse-variable-group-terraform # Variable Group from your image
- name: azureSubscription
  value: 'tss_d365_devops_dev_01'        # Service Connection Name from your image
- name: terraformVersion
  value: '1.9.0'

jobs:
- job: Terraform_Deployment
  displayName: 'Terraform Step-by-Step'
  steps:
  - checkout: self

  - bash: |
      sudo apt-get update && sudo apt-get install -y gnupg software-properties-common
      wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg > /dev/null
      echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
      sudo apt-get update && sudo apt-get install terraform=$(terraformVersion)
    displayName: 'Install Terraform $(terraformVersion)'

  - task: AzureCLI@2
    displayName: 'Terraform Init'
    inputs:
      azureSubscription: $(azureSubscription)
      scriptType: 'bash'
      scriptLocation: 'inlineScript'
      addSpnToEnvironment: true
      inlineScript: |
        set -e
        terraform init \
          -backend-config="environments/${{ parameters.environment }}/${{ parameters.environment }}.backend"
    
  - task: AzureCLI@2
    displayName: 'Terraform Plan'
    inputs:
      azureSubscription: $(azureSubscription)
      scriptType: 'bash'
      scriptLocation: 'inlineScript'
      addSpnToEnvironment: true
      inlineScript: |
        set -e
        export TF_VAR_sql_password=$(SYNAPSE_ADMIN_PASSWORD)
        terraform plan \
          -lock-timeout=120s \
          -var-file="environments/${{ parameters.environment }}/${{ parameters.environment }}.tfvars" \
          -out=tfplan

  - task: AzureCLI@2
    displayName: 'Terraform Apply'
    inputs:
      azureSubscription: $(azureSubscription)
      scriptType: 'bash'
      scriptLocation: 'inlineScript'
      addSpnToEnvironment: true
      inlineScript: |
        set -e
        export TF_VAR_sql_password=$(SYNAPSE_ADMIN_PASSWORD)
        terraform apply -lock-timeout=120s -auto-approve tfplan
